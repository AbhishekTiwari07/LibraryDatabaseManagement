const returnBook = document.getElementById('return')
const status = document.getElementById('status')

returnBook.addEventListener('click', () => {
    //initial condition of status is green if within the time limit else red
})